'use strict';

// Base libraries
import React from 'react';
import ReactDOM from 'react-dom';

// Components
import App from '../app';

ReactDOM.render(
  <App />,
  document.getElementById('app')
);
